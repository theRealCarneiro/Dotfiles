#!/bin/sh
echo url="https://www.duckdns.org/update?domains=therealcarneiro&token=$(pass duckdns/therealcarneiro)&ip=" | curl -k -o ~/.cache/duckdns/duck.log -K - && \
	notify-send "duckdns" "$(cat $HOME/.cache/duckdns/duck.log)"
