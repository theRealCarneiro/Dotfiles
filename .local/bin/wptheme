#!/bin/sh
wal_themes="$HOME/.config/wal/colorschemes"
wall_list=$(cat $wal_themes/dark/* $wal_themes/light/* | \
	grep wallpaper | cut -c16-  | \
	sed 's/..$//' | sort | uniq)

sxiv -t $wall_list -N theme_prompt
