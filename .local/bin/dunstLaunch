#!/bin/sh 
. /home/gabriel/.cache/wal/colors.sh
dunst -lf  "${main}" \
      -lb  "${background}" \
      -lfr "${background}" \
      -nf  "${main}" \
      -nb  "${background}" \
      -nfr "${background}" \
      -cf  "${main}" \
      -cb  "${background}" \
      -cfr "${main}" 
