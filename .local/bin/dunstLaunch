#!/bin/sh 
. /home/gabriel/.cache/wal/colors.sh
dunst -lf  "${color1}" \
      -lb  "${background}" \
      -lfr "${background}" \
      -nf  "${color1}" \
      -nb  "${background}" \
      -nfr "${background}" \
      -cf  "${color1}" \
      -cb  "${background}" \
      -cfr "${color1}" 
