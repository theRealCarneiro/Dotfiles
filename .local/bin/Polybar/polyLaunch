# !/bin/bash
export bg_alpha="#cf${background###}"
#export bg_alpha="#${background###}"
killall polybar 
if type "xrandr"; then
  for m in $(xrandr --query | grep " connected" | cut -d" " -f1); do
    MONITOR=$m polybar --reload bar &
  done
else
  polybar --reload bar &
fi
